version="1.0.1-SNAPSHOT"

apply plugin:'application'
mainClassName = 'au.com.breakpoint.hedron.daogen.DaoGen'

dependencies
{
    compile project (':hedron-core')

    testCompile 'junit:junit-dep:4.10'
}
